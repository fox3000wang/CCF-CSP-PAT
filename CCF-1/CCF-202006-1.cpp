#include <bits/stdc++.h>
using namespace std;
using gg = long long;
int main()
{
  freopen("CCF-202006-1.txt", "r", stdin);

  ios::sync_with_stdio(false);
  cin.tie(0);

  gg n, m;
  cin >> n >> m;

  vector<gg> x(n), y(n);
  for (gg i = 0; i < n; i++)
  {
    cin >> x[i] >> y[i];
  }

  gg a, b, c;
  for (gg i = 0; i < m; i++)
  {
    cin >> a >> b >> c;
    }

  //cout << o;
  return 0;
}